---
import { href } from '../lib/site';
import { getLocale, useTranslations } from '../i18n';

const locale = getLocale(Astro);
const t = useTranslations(locale);
---

<section class="section-tight sponsors" data-section="sponsors">
  <div class="container">
    <p class="label">{t('sponsors.label')}</p>
    <ul>
      <li>
        <a href="https://jb.gg/OpenSource" rel="noopener" target="_blank" aria-label="JetBrains" data-ga="sponsor-jetbrains">
          <img src={href('/img/sponsors/jetbrains.png')} alt="JetBrains" width="150" height="84" loading="lazy" />
        </a>
      </li>
      <li>
        <a
          href="https://www.ej-technologies.com/products/install4j/overview.html"
          rel="noopener"
          target="_blank"
          aria-label="install4j"
          data-ga="sponsor-install4j"
        >
          <img src={href('/img/sponsors/install4j.png')} alt="install4j" width="120" height="34" loading="lazy" />
        </a>
      </li>
    </ul>
    <p class="note">{t('sponsors.note')}</p>
  </div>
</section>

<style>
  .sponsors {
    text-align: center;
    border-top: 1px solid var(--border-2);
  }
  .label {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--fg-3);
    margin-bottom: 24px;
  }
  ul {
    list-style: none;
    margin: 0 0 16px;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 48px;
    flex-wrap: wrap;
  }
  img {
    opacity: 0.85;
  }
  a:hover img {
    opacity: 1;
  }
  .note {
    color: var(--fg-3);
    font-size: 0.85rem;
    margin: 0;
  }
</style>
